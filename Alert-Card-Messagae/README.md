# Update Available Alert

A modern, highly polished notification component featuring advanced CSS masking, smooth entry animations, and a clean "Inter" typography layout.

## 👀 Preview (Visual Design)

![Preview](update-preview.png) 

## 🚀 Features
* **Advanced CSS Masking:** Uses `radial-gradient` masks to create a unique wavy edge decorative border.
* **Dynamic Entry:** Integrated `@keyframes` for a smooth `slideInRight` entrance effect.
* **Design Tokens:** Built with CSS variables (`:root`) for instant color and theme customization.
* **Interactive UI:** Subtle lift effects on hover and transition-ready close buttons.
* **Accessibility:** Includes `aria-label` support for screen readers.

## 🛠️ Files
* `index.html` - The semantic structure of the alert.
* `UpdateAvailable.css` - Custom styling, animations, and design tokens.

## 🎨 Design Tokens (Colors)
* **Primary Info:** `#0EA5E9` (Sky Blue)
* **Background:** `#E0F2FE` (Light Info Tint)
* **Text (Main):** `#0369A1` (Deep Blue)
* **Shadow:** Soft elevation using `rgba(0, 0, 0, 0.08)`

## 📖 How to Use
1.  Clone or download the files to your local directory.
2.  Link the `UpdateAvailable.css` in your project's `<head>`.
3.  Add the `alert-card` HTML block wherever you need a system notification.
4.  Update the text inside `.alert-title` and `.alert-subtitle` to fit your needs.

