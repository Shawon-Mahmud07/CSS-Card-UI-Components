# 🪪 Professional Animated Identity Card

A high-fidelity, single-file UI component featuring a professional deep-navy aesthetic and kinetic CSS animations. This card is engineered for performance using a self-contained HTML/CSS architecture, featuring glowing corner accents and smooth state transitions.

## 👀 Preview

| Initial State | Hover Interaction |
| :--- | :--- |
| ![Static Preview](preview.png) | ![Hover Preview](preview-hover.png) |

## 🚀 Features
* **Encapsulated Design:** 100% self-contained in a single `index.html` file for easy deployment and portability.
* **Kinetic Hover Effects:** Intelligent scaling and shadow depth adjustments using CSS `transform` and `transition` properties.
* **Dual-Tone Accents:** Dynamic corner glows achieved via CSS pseudo-elements (`::before` and `::after`) for a modern "Neon-Edge" look.
* **Entrance Orchestration:** Smooth "Fade-In-Up" animation on load for a polished user experience.
* **Zero Dependencies:** No external libraries, frameworks, or JavaScript required.

## 🛠️ Files
* `index.html` - Contains the semantic structure and internal `<style>` block for the component.

## 🎨 Design Assets & Technologies
* **Framework:** HTML5 & CSS3 (Internal Styles)
* **Color Palette:** - **Background:** `#0b132b` (Deep Navy)
  - **Primary Accent:** `#00d2ff` (Electric Blue)
  - **Secondary Accent:** `#ff00ff` (Vibrant Magenta)
* **Animations:** CSS Keyframes (`fadeInUp`) for entrance orchestration.

## 💻 How to Use
1. Clone the repository or download the `index.html` file.
2. Open `index.html` in any modern web browser.
3. To customize the identity, simply edit the `h2` and `p` tags within the `.card-content` div located in the HTML.